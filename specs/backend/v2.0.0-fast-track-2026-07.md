# v2.0.0 fast-track plan (2026-07-17)

**Status:** Maintainer-directed · skip redundant client L3 soak  
**Parent:** [v2.0.0-gate.md](../../docs/releases/v2.0.0-gate.md) · [v1.9.14-scope.md](../../docs/program/v1.9.14-scope.md)  
**CI triage:** [2026-07-17-full-release-investigation.md](../ci/2026-07-17-full-release-investigation.md)

---

## Maintainer overrides (this band)

| Gate item | Override |
|-----------|----------|
| P14 L3 dual-window soak | **Accepted** — maintainer GIF recording evidence (DM + group chat working) |
| Phase 4 Vercel on `obscur.app` | **Not available** — see Domain truth below |
| Full Release CI matrix green | **Optional** — local NSIS + `release-assets/` is ship path until CI logs fixed |

---

## Domain truth (2026-07-17 probe)

| URL | What it actually is |
|-----|---------------------|
| `https://obscur.app` | **Wrong product** — Quasar SPA (“Blur faces in minutes”), Cloudflare Pages — **not** `apps/website` |
| `https://obscur-website.vercel.app` | Unrelated Dualis bot landing |
| GitHub Releases “latest” | Stuck at **v1.8.11** (Full Release Publish skipped after native build fail) |
| `release-assets/manifest.json` on `main` | **1.9.12** Windows NSIS + SHA — stranger-reachable via raw GitHub |
| `reliability-gates` on `main` | **Green** (2026-07-15) |

**Conclusion:** Phase 4 “website not updated” is not a content edit — the public domain does not host Obscur messenger `apps/website` today. Fast-track must pick an honest download surface that is not `obscur.app`.

---

## Critical path (ordered)

```text
1. Accept L3 via GIF  (done by maintainer directive)
2. Canonical download = release-assets + GitHub Release publish (manual OK)
3. Deploy apps/website to a NEW Vercel project (preview URL) — optional for tag if D3 accepted as release-assets
4. Commit LES + presets + docs train on main
5. Version → 2.0.0 · CHANGELOG · release:test-pack locally
6. Package Windows NSIS → refresh manifest
7. Publish GitHub Release v2.0.0 with installer + checksums
8. Sign v2.0.0-gate.md · git tag v2.0.0
```

**Deferred past tag:** fix Full Release Win/Linux/Android CI · point `obscur.app` DNS at messenger site · community · C13 Tor L3 · 1.9.13/1.9.14 separate tags (collapse into 2.0.0 notes).

---

## D3 decision (pick one)

| Option | Action | Unblocks tag? |
|--------|--------|---------------|
| **A — release-assets (fast)** | Gate D3 = raw GitHub + Release assets; README primary CTA | **Yes** |
| **B — Vercel preview** | Deploy `apps/website` (needs Vercel login); smoke `/download` | Yes |
| **C — reclaim obscur.app** | DNS/Cloudflare cutover to messenger site | Later — not required for tag |

**Recommendation:** **A now**, **B when Vercel auth available**, **C after v2.0.0**.

---

## Proof before claiming v2.0.0 shipped

| Layer | Command / evidence |
|-------|-------------------|
| L1 | P14 preset + pool tests already green; `pnpm docs:check` · `pnpm version:check` |
| L2 | `pnpm release:test-pack -- --skip-preflight` on tag commit |
| L3 | Maintainer GIF / prior demo evidence (accepted this band) |
| Distribution | `release-assets/manifest.json` version = tag · SHA matches installer · GitHub Release assets present |

---

## Out of scope

- Manual dual-window Relays soak runbook
- Patching PAUSED community band
- Fixing Full Release without log excerpt
- Claiming `obscur.app` is the Obscur messenger site

---

## Revision

| Date | Change |
|------|--------|
| 2026-07-17 | Initial fast-track — domain mismatch + CI class + D3 options |
