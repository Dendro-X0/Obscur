# v1.8.3 — Lane T: REL-004 leave durability

**Status:** **Complete** — tagged **v1.8.3** (2026-05-27)  
**Predecessor:** [v1.8.2-release.md](../releases/v1.8.2-release.md)  
**Program:** [obscur-2.0-milestone-roadmap.md](./obscur-2.0-milestone-roadmap.md) · Lane **T**

---

## Goal

**REL-004:** A user's **leave decision is durable locally** before any relay publish. Relay failure, rate limits, or offline windows must not roll back **left** membership truth or resurrect the community as joined.

Baseline already on `main` (2026-05-15 slices): `community-leave-outbox.ts`, background retry in `RuntimeActivationManager`, ledger-first leave in `GroupProvider` / sealed leave / settings bulk-leave.

---

## Done when (REL-004 exit)

| ID | Task | Evidence |
|----|------|----------|
| T4-1 | **Canonical leave paths** all enqueue outbox **before** relay attempt | **Done** — `community-leave-path-audit.test.ts`; `removeGroupConversation` aligned |
| T4-2 | **Left** is authoritative in UI immediately | Leave confirmation → community absent from Network/groups list without waiting for relay |
| T4-2b | **Observer roster** drops leavers; re-invite enabled | **Fix:** sealed `leftMembers` beats chat author evidence (MEM-002) |
| T4-3 | **Pending relay publish** visible when outbox item exists | **Done** — Network banner + group card badge + honest copy module |
| T4-4 | **Background retry** clears or surfaces terminal reject | `useCommunityLeaveOutboxRetry` + demo matrix Pass |
| T4-5 | Demo matrix [v1.8.3](../assets/demo/v1.8.3/README.md) Pass | Tester 1 dark / Tester 2 light — simulate relay block then recovery |

---

## Out of scope (v1.8.4+)

- **Lane K / v1.9.x** kernel refactor — chartered in [v1.9.0-kernel-backend-roadmap.md](./v1.9.0-kernel-backend-roadmap.md); start after v1.8.3 tag.
- **REL-001** remaining resurrection paths (stale joined ledger vs leave intent).
- **REL-003** multi-profile cross-read (separate patch).
- **REL-005** mutation-owner convergence.
- **MEM-002 … MEM-006** (next Lane T batch after REL-004 exit).
- Lane X / Lane P.

**MEM-001 park** unchanged — no new roster features.

---

## Implementation notes

| Owner | Module |
|-------|--------|
| Durable intent | `community-leave-outbox.ts`, `community-membership-leave-intent.ts` |
| Retry transport | `community-leave-outbox-retry.ts`, `use-community-leave-outbox-retry.ts` |
| Leave UX | `groups/leave`, `group-provider.tsx`, `use-sealed-community.ts`, settings bulk-leave |

One canonical **leave mutation owner** per path: ledger/outbox first, relay second.

---

## Version policy

Tag **`v1.8.3`** when REL-004 exit + `pnpm release:test-pack` green on clean `main`.
