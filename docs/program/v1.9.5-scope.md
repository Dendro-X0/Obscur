# v1.9.5 — B4a R1 DM materialization port convergence

**Roadmap:** [v1.9.0-kernel-backend-roadmap.md](./v1.9.0-kernel-backend-roadmap.md) · **Spec:** [v1.9.0-kernel-backend-spec.md](./v1.9.0-kernel-backend-spec.md) §B4 / R1

## Goal

`use-conversation-messages` and feature code route DM thread assembly through `messagingClientOperations` → `dmConversationMaterialization` — no parallel suppression/merge imports.

## Delivered (this slice)

| Item | Detail |
|------|--------|
| Port methods | `filterThreadMessagesBySuppression`, `mergeHydratedBaseWithLiveOverlay` on materialization owner |
| Facade | `messagingClientOperations.filterDmThreadMessagesBySuppression`, `mergeDmHydratedBaseWithLiveOverlay` |
| Hook | `use-conversation-messages` no longer imports `conversation-message-materialization` |
| Boundaries | ESLint + `pnpm gateway:boundaries:check` guard `conversation-message-materialization` + `prepareDmThreadSuppressionIds` |

Hydrate / projection / realtime / load-earlier already routed via port (prior R1 milestones).

## Exit

- DM-001: `dm-delete-reload-projection.test.ts` green (delete-for-me survives simulated reload).
- Manual K-M5 when tagged.

## Next

- Lane K band exit: tag v1.9.x, manual matrix K-M1–K-M6.
- Optional: move `use-conversation-messages.test.ts` realtime cases behind port facade only.
