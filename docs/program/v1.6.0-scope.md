# v1.6.0 — Phase 2: governance projection owner

**Status:** **Shipped** — tag **v1.6.0** (2026-05-21)  
**Predecessor:** [v1.5.8-scope.md](./v1.5.8-scope.md) (shipped)  
**Roadmap:** [community-system-overhaul-phased-roadmap.md](./community-system-overhaul-phased-roadmap.md) Phase 2  
**Feature roadmap:** [v1.5.x-feature-roadmap.md](./v1.5.x-feature-roadmap.md) (v1.6.0 charter)

---

## Goal

Elevate community **governance** into a first-class projection surface (contract 26) instead of hook-local-only state in `use-sealed-community`. Keep Phase 1 membership owners in **park mode** until R2.

**Bar for success:**

- **2.1** — `governanceByCommunityId` (or equivalent) is the single read owner for governance UI; hook holds ingest only.
- **2.2** — Lifecycle completeness (ties, dedupe, TTL) verified in projection replay tests, not only hook reducer.
- **`pnpm release:test-pack`** green before tag.

---

## Track G — Governance projection (Phase 2)

| ID | Task | Done when |
|----|------|-----------|
| G2.1 | **Projection owner** | **Done on `main`:** `community-governance-projection` + `useCommunityGovernanceProjection`; UI no longer reads hook `state.governance` |
| G2.2 | **Lifecycle completeness** | **Done on `main`:** `replayCommunityGovernanceEvents` + projection replay tests (tie, duplicate `resolved`, TTL, reorder); `activeVotes` filters expired |
| G2.3 | **Ledger reasons** | **Done:** `governance_descriptor_accepted`, `governance_member_expelled`; `community-governance-mutation-owner` + bus `governanceProposalId` |
| G2.4 | **Regression suite** | **Done:** [v1.6.0 demo matrix](../assets/demo/v1.6.0/README.md) + multi-device quorum replay test |

---

## Track R — Carryover (refine only)

| ID | Task | Notes |
|----|------|-------|
| R5 | Membership edge cases | **Park** per [community-membership-invariants.md](./community-membership-invariants.md) — invariant fixes only |
| R2 | Single roster read owner | Architecture queue — not required for v1.6.0 tag unless unblocks governance UI |

---

## Track N — v1.6 charter (optional same tag)

| ID | Task | Notes |
|----|------|-------|
| N1 | Recall / redaction UI | From feature roadmap — schedule only if governance track is stable |

---

## Non-goals

- New membership roster features (MEM-001 park).
- Lane **M** store/signing matrix.
- P2 managed workspace / P3 tabbed shell (Phase 3–4).
- Kernel split / full storage port rewrite.

---

## Version bump

`1.6.0` via `pnpm version:bump minor` + `pnpm version:check`.

---

## Closeout

`CHANGELOG.md` `## [v1.6.0]`, [v1.6.0-gate.md](../releases/v1.6.0-gate.md), handoff → Phase 2 exit or v1.6.1 patch.
