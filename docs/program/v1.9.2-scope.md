# v1.9.2 — Lane K (B2: Coordination directory)

**Status:** **Done on `main`** — ships in **v1.9.0** tag (B2); manual K-M Pass 2026-06-01  
**Predecessor:** [v1.9.1-scope.md](./v1.9.1-scope.md) (B1)  
**Spec:** [v1.9.0-kernel-backend-spec.md](./v1.9.0-kernel-backend-spec.md) §B2

---

## Goal

Cross-client membership fanout via **coordination backend** (signed deltas), not public relay roster. B's client applies leave within poll interval when `NEXT_PUBLIC_COORDINATION_URL` is set.

---

## In scope (B2)

| ID | Task | Status |
|----|------|--------|
| K-B2-1 | D1 schema + worker routes (`head`, `deltas`, `delta`) | **Done** |
| K-B2-2 | `@dweb/coordination-contracts` sign/verify | **Done** |
| K-B2-3 | `@dweb/transport-coordination` semantic mapping | **Done** |
| K-B2-4 | PWA client publish/poll + cursor | **Done** |
| K-B2-5 | `community-membership-sync.ts` coordination poll | **Done** |
| K-B2-6 | `use-sealed-community` subscribe + leave publish | **Done** |
| K-B2-7 | Tests + manual matrix K-M1/K-M2 | Pending manual |

---

## Configuration

- `NEXT_PUBLIC_COORDINATION_URL` — e.g. local `http://127.0.0.1:8787` (`pnpm -C apps/coordination dev`)
- Run migration: `pnpm -C apps/coordination db:migrate` (includes membership tables)
- Mode: `coordination_preferred` when URL set (override via `obscur.membership_sync_mode.v1` in localStorage)

---

## Done when

1. Demo matrix [v1.9.0](../assets/demo/v1.9.0/README.md) K-M1/K-M2 Pass with coordination running.
2. Tag **v1.9.2** after tests green.

---

## Next

**v1.9.3 B3** — UI honesty + membership sync mode in Settings.
