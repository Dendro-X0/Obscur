# v1.8.14 — Batch exit: P13 + B2 + Wave 3 trust

**Status:** Active (batch exit candidate)  
**Predecessor:** [v1.8.13-scope.md](./v1.8.13-scope.md) (R13 CI — may ship separately or roll into this tag)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md) · [v1.8.x-batch-implementation-lane.md](./v1.8.x-batch-implementation-lane.md)  
**Gate:** [v1.8.14-gate.md](../releases/v1.8.14-gate.md)

---

## Goal

One **user-visible** patch tag after the v1.8.x batch implementation lane — mobile polish (P13), inbound community bots (B2), and trust slices (REL/MEM). Manual demo matrices remain deferred; automated gates must be green.

**Do not publish void `v1.8.12`.** Production GitHub Release remains **v1.8.11** until maintainer runs Full Release for **v1.8.14** (or **v1.8.13** if R13 is tagged alone first).

---

## In scope

| Wave | ID | Deliverable | Status |
|------|-----|-------------|--------|
| **1** | P13 | Mobile shell polish — status strip, thread header, compact cards, chat-thread previews/timestamps, composer banners, action dock | **Done** (P13-x scroll/Framer partial) |
| **2** | B2 | Inbound bot triggers — descriptor contract, runner, steward UX, runbook | **Done** |
| **3** | REL-003 | Multi-profile ledger isolation — legacy seed leak fixed; bootstrap restore preserved | **Partial** — memory caches deferred |
| **3** | REL-004 | Leave outbox durability + path audit | **Done** |
| **3** | MEM-002 | Join/leave read-model convergence (terminal cache in index hook) | **Partial** — manual cross-surface soak deferred |
| **3** | MEM-005 | Inviter terminal invite clears relay-joined roster | **Done** |
| **R** | R12/R13 | Release artifact parity + simplified Full Release workflow | On `main` (carried from v1.8.12–v1.8.13) |

---

## Accepted limitations (no incremental work in this tag)

| ID | Theme |
|----|--------|
| DM-001 | One-sided DM outgoing history on restore |
| MEM-001 | (parked with DM-001) |

---

## Out of scope

- **P14** — mobile settings / network layout (Wave 4)
- **REL-003** remaining vectors — invite snapshot memory cache, community-sync keyed by community only
- **Lane K / v1.9.0** — after v1.8.x batch exit recorded
- Per-step manual demo **Pass** columns (see [deferred-manual-verification-checklist.md](./deferred-manual-verification-checklist.md))

---

## Release workflow (maintainer)

1. Push `main` → **reliability-gates** green (`release:test-pack`).
2. Optional: **Actions → Obscur Full Release → Run workflow** on `main` (no tag burn).
3. When ready: `git tag -a v1.8.14 -m "v1.8.14 — P13 mobile polish, B2 bots, trust slices"` && push tag.
4. Publish with `publish_release=true` only after verify is green.
