# v1.9.4 — B4b R2 participant roster UI convergence

**Roadmap:** [v1.9.0-kernel-backend-roadmap.md](./v1.9.0-kernel-backend-roadmap.md) · **Spec:** [v1.9.0-kernel-backend-spec.md](./v1.9.0-kernel-backend-spec.md) §B4 / R2

## Goal

Community participant **display** lists use the monotonic R2 read model so relay warm-up snapshots cannot collapse the UI to a single member (MEM-001).

## Delivered (this slice)

| Item | Detail |
|------|--------|
| Group home | Online/offline + member count use `rosterDisplayPubkeys` from `useCommunityParticipantRosterReadModel` |
| Group management | Members panel uses the same hook for `visibleMemberRegistry` |
| Roster port | `resolveParticipantRosterReadModel` on `communityRoster` gateway owner |

Invite/send gates still use `inviteEligibleMemberPubkeys` (active + terminal policy).

## Exit

- MEM-001 manual scenario: reload group home — participant count does not collapse to 1 when relay snapshot thins.
- `community-participant-roster-read-model` tests green; group-provider roster tests unchanged.

## Next

- **v1.9.5 B4a** — [v1.9.5-scope.md](./v1.9.5-scope.md) (R1 hook facade).
- `group-provider.tsx` reads roster projection only via port (full R2 engineering exit — optional follow-up).
