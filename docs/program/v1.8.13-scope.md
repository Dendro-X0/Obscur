# v1.8.13 — Lane R: simplified release CI + carry R12/P12

**Status:** Superseded by [v1.8.14-scope.md](./v1.8.14-scope.md) for batch exit tagging. R13 CI changes roll into **v1.8.14**.
**Predecessor:** [v1.8.12-scope.md](./v1.8.12-scope.md) (**void tag** — do not publish `v1.8.12`)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md)  
**Gate:** [v1.8.13-gate.md](../releases/v1.8.13-gate.md)

---

## Goal

1. **R13** — Ship release artifact parity (desktop + Android) on a **fresh tag** without re-burning failed tags.
2. **CI** — Simpler Obscur Full Release: re-run builds via **workflow_dispatch**, one verify step, no duplicate `release:test-pack` in release preflight.
3. **P12 carry** — Android smoke/UX evidence from v1.8.12 demo matrix (manual).

**B2 inbound bot triggers** remain **v1.8.14+** unless explicitly pulled in.

---

## Why v1.8.12 is void

`v1.8.12` on `main` included R12 fixes, but **GitHub Release** stayed on **v1.8.11** after verify/publish friction. Tags are immutable — **do not re-tag v1.8.12**. Use **v1.8.13** only.

---

## In scope

| ID | Task | Status |
|----|------|--------|
| R13-1 | `pnpm version:bump patch` → **1.8.13** | Done |
| R13-2 | Simplified `release.yml` (dispatch rebuild, merged verify) | Done |
| R13-3 | AAB parity fallback (binary + APK metadata) | Done on `main` |
| R13-4 | Tag `v1.8.13` + publish after green Full Release | Pending |

---

## Release workflow (maintainer)

1. Push fixes to `main` → **reliability-gates** runs (`release:test-pack`).
2. **Actions → Obscur Full Release → Run workflow** (no new tag) — builds + verify.
3. When green: tag once — `git tag -a v1.8.13 -m "..." && git push origin v1.8.13`  
   **Or** dispatch with `publish_release=true` and `release_tag=v1.8.13` from `main` after verify.
4. Publish only when verify is green.

---

## Out of scope

- B2/B3 inbound bots (**v1.8.14+**)
- Lane K / **v1.9.0**
