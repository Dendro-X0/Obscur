# v1.9.3 — B3 copy audit checklist

**Goal:** UI matches sovereign vs coordinated guarantees; no false “live global roster” or “relay-confirmed member” semantics.

**Demo matrix:** [v1.9.0/README.md](../assets/demo/v1.9.0/README.md) rows K-M3, K-M4.

---

## Automated checks

```bash
pnpm -C apps/pwa exec vitest run \
  app/features/groups/utils/community-membership-evidence-display.test.ts \
  app/features/groups/services/community-directory-materialization-policy.test.ts \
  app/features/groups/services/community-legacy-sovereign-policy.test.ts \
  app/features/groups/components/create-group-dialog.test.tsx \
  app/features/settings/components/community-membership-sync-settings-panel.test.tsx
```

---

## Copy audit (manual)

| ID | Surface | Pass when |
|----|---------|-----------|
| B3-01 | Membership evidence chips (`nostr_only`) | Relay-backed tier shows **Relay hint**, not “confirmed” |
| B3-02 | Membership evidence chips (`coordination_preferred`) | Relay-backed tier shows **Directory** |
| B3-03 | Provisional chip | Hint mentions coordination when sync mode is coordination |
| B3-04 | Group home / manage — directory honesty banner | Shown when `claimsAuthoritativeDirectory` is false |
| B3-05 | Legacy sovereign on public relay | Amber **Legacy sovereign room** notice visible |
| B3-06 | Create group — public relay baseline | Amber panel: no live roster parity + workspace-only note (K-M4) |
| B3-07 | Create group — `nos.lol` host | Create blocked; workspace blocked panel |
| B3-08 | Settings → membership sync | R1 lock copy when coordination URL set |
| B3-09 | Leave (`nostr_only` / sovereign path) | Info toast: excluded locally; others may see relay hints (K-M3) |
| B3-10 | Managed workspace + coordination | Directory summary claims **Coordination membership directory** on private/intranet tier |

---

## Product positioning (privacy teams & interest groups)

| Audience | Mode | Honest promise |
|----------|------|----------------|
| Work team | `managed_workspace` + coordination | Membership directory on operator coordination; E2E chat on private relay |
| Interest group | Same stack, smaller host | Same architecture; operator may be a member’s machine (see [private-trust-local-setup.md](../assets/demo/private-trust-local-setup.md)) |
| Legacy / public | `sovereign_room` on public relay | Read-oriented; best-effort hints only |

**Out of scope for v1.9.3:** team video / SFU (future band).

---

## Sign-off

| Row | Owner | Date | Pass |
|-----|-------|------|------|
| K-M3 | | | |
| K-M4 | | | |
| Automated vitest | CI / Phase A | 2026-06-01 | **Done** — see § Automated checks |
