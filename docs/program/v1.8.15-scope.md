# v1.8.15 — Lane X: experience polish (X1–X3)

**Status:** Active — X1–X3 implemented (2026-06-03)  
**Predecessor:** [v1.8.14-scope.md](./v1.8.14-scope.md)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md) · Lane **X**

---

## Goal

Ship user-visible experience polish without reopening navigation perf owners:

- **X1** — Visual warm-up skeleton/stagger during route chunk load (not prefetch alone)
- **X2** — Page transition loading shell during sidebar navigation
- **X3** — Voice call dock polish (elapsed timer + ring pulse for pre-connect phases)
- **X4–X6** — Image / audio / video preview — **existing** `lightbox.tsx`, `audio-player.tsx`, `video-player.tsx` + `media-preview-*` CSS (no new owner)

---

## Done when (v1.8.15 exit)

| ID | Task | Evidence | Status |
|----|------|----------|--------|
| X1 | Route warm-up skeleton with surface-aware stagger | `route-warmup-skeleton.tsx`, wired via `route-loading-fallback.tsx` | **Done** |
| X2 | Page transition loading shell on pathname change | `page-transition-loading-shell.tsx` in `app-shell.tsx` | **Done** |
| X3 | Voice call pre-connect elapsed timer + ring pulse | `voice-call-dock.tsx` | **Done** |
| X4–X6 | Media preview components | Existing lightbox + players (v0.8.3 gate) | **Accepted** — no duplicate owner |

---

## Guardrails

- Respect `shouldRunNavigationInstrumentation()` — experiment shell skips transition/skeleton overhead.
- Do not add parallel route prefetch paths; visual warm-up only.
- `motion-reduce` / `motion-safe` on all new animation.

---

## Out of scope

- Lane P / SQLite / Android
- Lane K coordination backend
- Per-nav manual demo Pass columns
