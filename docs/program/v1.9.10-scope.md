# v1.9.10 — Desktop manual-unlock policy + version truth

**Status:** Complete (2026-06-17)  
**Predecessor:** v1.9.9 storage resilience  
**Related:** [v1.9.6-session-persistence-redesign.md](../archive/program/inactive-2026-06/v1.9.6-session-persistence-redesign.md) · [v1.9.10-release.md](../releases/v1.9.10-release.md)

---

## Theme

Close AUTH-SESSION-1 by **product subtraction** (not patch loops) and **recover semver/doc truth** for bands 1.9.4–1.9.9 that shipped on `main` without manifest updates.

---

## In scope (complete)

| Item | Outcome |
|------|---------|
| Cancel desktop keychain restore | `NATIVE_SECURE_SESSION_RESTORE_ENABLED = false` on desktop shell |
| Cancel stay-signed-in persist | `NATIVE_DEVICE_SESSION_CONSENT_ENABLED = false` on desktop shell |
| Consent gates | `isDeviceSessionRestoreAllowed` / `resolveStaySignedIn` respect policy |
| UI honesty | Manual-unlock copy; hide device session panel on desktop |
| Version sync | Root `package.json`, `version.json`, workspace manifests → **1.9.10** |
| CHANGELOG + train | Entries for 1.9.4–1.9.10; [v1.9.x-release-train.md](./v1.9.x-release-train.md) updated |

---

## Verification

```bash
pnpm verify:session-persistence-policy
pnpm version:check
```

---

## Out of scope

- Rust-first boot hydrate (future AUTH-SESSION-1 revival only)
- Git tags v1.9.4–v1.9.10 (maintainer action after manual gates)
