# v1.9.0 — Lane K band opener (B0: Transport boundary)

**Status:** **In progress** — B0 code on `main`; tag after v1.8.3 or with B0-only bump to **1.9.0**  
**Roadmap:** [v1.9.0-kernel-backend-roadmap.md](./v1.9.0-kernel-backend-roadmap.md)  
**Spec:** [v1.9.0-kernel-backend-spec.md](./v1.9.0-kernel-backend-spec.md)  
**Gate:** [v1.9.0-gate.md](../releases/v1.9.0-gate.md)

---

## Goal

Establish **TransportPort** and stop feature-layer coupling to Nostr kinds. No user-facing “new feature” marketing — this patch is **structural** so v1.9.1+ can move membership truth into the kernel.

---

## In scope (B0 only)

| ID | Task | Exit evidence |
|----|------|---------------|
| K-B0-1 | Add `packages/dweb-transport-contracts` with `TransportPort` + semantic event types | **Done** |
| K-B0-2 | Add `NostrTransportAdapter` + sealed control mapper | **Done** |
| K-B0-3 | Wire `communityTransport` through ClientGateway / profile runtime | **Done** |
| K-B0-4 | `pnpm transport:boundaries:check` + baseline allowlist (47 files) | **Done** |
| K-B0-5 | `use-sealed-community` leave/join/restate via semantic ingress | **Done** |

---

## Out of scope (v1.9.0)

- Coordination membership API (B2 / v1.9.2).
- UI badge renames (B3).
- R1/R2 full convergence (B4).
- User-visible transport switch in Settings (stub OK; full UX in v1.9.3).

---

## Done when

1. All K-B0-* rows **Done** in [v1.9.0-gate.md](../releases/v1.9.0-gate.md).
2. `pnpm release:test-pack` green on tagged commit.
3. Handoff: **Next band patch = v1.9.1 (B1)**.

---

## Version policy

- Tag **`v1.9.0`** when B0 exit only — not a bundle of B1/B2.
- Bump with `pnpm version:bump minor` from last v1.8.x patch (opens **1.9.0** band per program policy).
