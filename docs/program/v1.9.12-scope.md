# v1.9.12 — Conduit Mesh + trust gates (demo-safe)

**Status:** Active release prep (2026-07-15)  
**Predecessor:** [v1.9.11-scope.md](./v1.9.11-scope.md)  
**Deferred:** Vault chat→save re-enable → [v1.9.13-scope.md](./v1.9.13-scope.md)

---

## Theme

Ship a **demo-safe desktop production package** that includes HTTP mesh DM (pull / long-poll / SSE), mesh-native wire, Tor SOCKS L1 (L3 env-blocked), WS mesh default soak evidence, trust/link/export gates, and voice-note UX — **without** flipping vault chat-save or unpausing community.

---

## In scope

| Track | Outcome |
|-------|---------|
| **Conduit Mesh C0–C14** | L1 gates green; C7 / C10 / C14 L3 soaks **PASS** |
| **C13 Tor SOCKS** | L1 only — L3 **BLOCKED** on this host (TLS bootstrap) |
| **Trust / ASE** | Action gates, link confirm, attachment export confirm |
| **Voice notes** | Inline player / waveform composer (landed in tree) |
| **Dev shell freshness** | Stamp mismatch banner + rebuild path |
| **Release** | Semver **1.9.12**, CHANGELOG/release notes, `release:test-pack`, `desktop:package` |

## Out of scope

| Item | Reason |
|------|--------|
| `VAULT_SAVE_FROM_CHAT_ENABLED = true` | Remains **false** — [v1.9.13](./v1.9.13-scope.md) |
| Community COM-RUN feature patches | **PAUSED** |
| Phase 4 Vercel deploy | **PAUSED** |
| `v2.0.0` tag | Separate gate |
| C13 L3 Tor onion DM soak | Environment-blocked |

---

## Honesty for demo / support

| Show | Avoid claiming |
|------|----------------|
| Dual-window DM on public WS (`nos.lol` / `damus`) | Tor-routed HTTP mesh on this host |
| HTTP-only mesh @ `127.0.0.1:8788` with SSE | Community roster convergence |
| Secure Upload → vault | Chat → Save to Vault (still disabled) |

---

## Verification

| Layer | Command / evidence |
|-------|-------------------|
| L1 | `pnpm verify:conduit-mesh-c14` · `pnpm verify:engine-lab` (subset ok) |
| Release | `pnpm release:test-pack` |
| Package | `pnpm desktop:package` |
| L3 prior | C7 / C10 / C14 runbook PASS rows |

---

## Exit

Tag **`v1.9.12`** after green release pack + maintainer package checksum update in `release-assets/manifest.json`.
