# v1.9.1 — Lane K (B1: Membership kernel port)

**Status:** **Done on `main`** — ships in **v1.9.0** tag  
**Predecessor:** [v1.9.0-scope.md](./v1.9.0-scope.md) (B0)  
**Spec:** [v1.9.0-kernel-backend-spec.md](./v1.9.0-kernel-backend-spec.md) §B1

---

## Goal

One **write path** for membership control events: `communityMembership` on ClientGateway. Hooks dispatch; port owns reducer semantics and terminal cache persistence API.

---

## In scope (B1)

| ID | Task | Status |
|----|------|--------|
| K-B1-1 | `CommunityMembershipPort` contract in `@dweb/client-gateway` | **Done** |
| K-B1-2 | `community-membership-port-owner.ts` — apply join/leave/expel/disband + semantic | **Done** |
| K-B1-3 | Gateway `communityMembership` wired in adapter | **Done** |
| K-B1-4 | `use-sealed-community` uses port for membership `applyControlEvent` + terminal persist | **Done** |
| K-B1-5 | Contract tests `community-membership-port.test.ts` | **Done** |
| K-B1-6 | Leave intent via `persistExplicitCommunityMembershipLeave` remains mutation-owner; port documents terminal path | **Done** (ledger path unchanged — B1.1 optional) |

---

## Out of scope (v1.9.1)

- Coordination directory (B2 / v1.9.2).
- Nostr `TransportPort` publish/subscribe wire-up.
- Full removal of `leftMembers` state from hook (UI still holds snapshot; mutations go through port only).

---

## Done when

1. K-B1-* rows Done in [v1.9.0-gate.md](../releases/v1.9.0-gate.md) §B1.
2. `pnpm test:community-invariants` + integration tests green.
3. Tag **`v1.9.1`** after B0 tagged (or combine band tag per maintainer choice).

---

## Next

**v1.9.2 B2** — coordination membership head/delta + `community-membership-sync.ts`.
