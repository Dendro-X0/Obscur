# Legacy Documentation Archive

This folder contains documentation that was superseded on 2026-03-03 during a full `/docs` rewrite.

- Reason: previous docs had mixed status (historical specs, stale plans, and active behavior docs in one layer).
- Policy: keep these files for historical context only.
- Active docs now live in the top-level `docs/*.md` files.

Do not treat legacy files as source of truth for current runtime behavior.
