[info]
relay_url = "ws://localhost:7000"
name = "Obscur Local Relay"
description = "Local relay for Obscur development and Playwright E2E"

[database]
data_directory = "/usr/src/app/db"

[network]
address = "0.0.0.0"
port = 8080

[limits]
messages_per_sec = 200
subscriptions_per_min = 200

[authorization]
# Open relay for local development
pubkey_whitelist = []
