# v1.3.8 Replay Media

Store short recordings proving manual replay outcomes:

1. offline boot + degraded/reconnect transitions,
2. in-app update success flow,
3. in-app update failure/blocked flow (bad signature or kill switch).
