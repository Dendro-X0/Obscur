# Example environment variables for Obscur PWA

# Optional.
# Used by some internal API wrappers.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787

# Optional.
# Coordination Worker base URL (Cloudflare Worker local dev default).
# Used for invite token create/redeem.
NEXT_PUBLIC_COORDINATION_URL=http://127.0.0.1:8787
