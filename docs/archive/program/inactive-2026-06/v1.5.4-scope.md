# v1.5.4 — One Mark (desktop polish + unified branding)

**Status:** Shipped (2026-05-18)  
**Program lanes:** Client stability & perf (desktop/web), installer branding  
**Strategic frame:** [strategic-direction.md](./strategic-direction.md)  
**Predecessor:** [v1.5.3 release](../releases/v1.5.3-release.md) (shipped)  
**User-facing release notes:** [v1.5.4-release.md](../releases/v1.5.4-release.md)  
**Gate checklist:** [v1.5.4-gate.md](../releases/v1.5.4-gate.md)

---

## Goal

Ship **v1.5.4** as a **desktop/web polish** release with **one Obscur mark** on every **ship surface** — especially Android (and verify macOS/Linux match Windows).

**Bar for success:**

- **Windows** already ships the Obscur mark (shortcuts, NSIS installer, in-app) — treat as baseline, do not regress.
- **Android** launcher / APK icon shows Obscur arcs (not Tauri default).
- **macOS / Linux** artifacts visually match Windows on next tag build.
- At least one additional client stability or relay discipline improvement with regression test.
- **Mobile device testing is suspended** for this version — no M1–M3 gate; APK may still build in CI but is not a ship blocker for maintainer sign-off.

---

## Non-goals

| Non-goal | Rationale |
|----------|-----------|
| Mobile device smoke matrix (M1–M3) | Suspended until disk/signing/device path is ready |
| Full mobile UI rewrite (DM-first layouts) | Deferred to v1.5.5+ when mobile testing resumes |
| Play Store / App Store ship | No store listing work |
| Code signing certificate purchase (Windows Authenticode / Play signing) | Document path; optional if secrets already exist |
| v1.6 cooperative recall UI | Separate lane |

---

## Track A — Cross-platform icon parity (required)

**Canonical mark:** `apps/pwa/public/obscur-logo-dark.svg` (same geometry as PWA `icon-512` route).

**Baseline (already true):** Windows desktop — shortcuts, installer, title bar / taskbar use Obscur arcs on dark rounded square (see maintainer screenshot).

**Gap (v1.5.4 fix):** Android `mipmap-*` / launcher still generated from Tauri template in many CI builds; macOS `.dmg` and Linux `.AppImage` need visual confirm on tag artifacts.

| Task | Owner | Done when |
|------|-------|-----------|
| BRAND-1 | Export 1024×1024 PNG from canonical SVG (match Windows look) | ✅ `icon-source.png` |
| BRAND-2 | `pnpm icons:regenerate` | ✅ landed `a2a998cc` |
| BRAND-3 | Diff Android mipmaps vs Windows shortcut | ✅ in repo; confirm on next APK |
| BRAND-4 | Spot-check macOS `.dmg` + Linux `.AppImage` on Release | At `v1.5.4` tag |
| BRAND-5 | `pnpm icons:regenerate` | ✅ |

**Note:** Unsigned APK/EXE warnings are expected until release signing secrets are configured; public GitHub provenance is documented in release notes — not a product defect.

---

## Track B — Client smooth (desktop + web, continue v1.5.3)

### WS-C — Relay quiet recovery

**Owner:** `relay-provider`, pool connect policy.

| Task | Done when |
|------|-----------|
| Reduce duplicate connects to same scoped URL where possible | ✅ `connectToRelay` reuses OPEN/CONNECTING sockets |
| Degraded state visible when publish path unavailable | ✅ chat banner + sidebar Degraded + composer block copy |

### WS-D — Pick one P1 from manual matrix or triage

| Task | Done when |
|------|-----------|
| One user-visible rough edge fixed with test | ✅ composer shows `APP_VERSION` (was hardcoded 0.4.0) |
| Android versionName aligns with monorepo | ✅ `tauri.properties` via `version:sync` |

---

## Track C — Mobile (version parity, production deferred)

**Policy:** [mobile-desktop-version-policy.md](./mobile-desktop-version-policy.md) — same `1.5.x` version as desktop; **mobile production release later**.

| Task | Done when |
|------|-----------|
| CI builds APK/AAB on same tag as desktop | Artifacts show `versionName` = monorepo version |
| Icons / metadata aligned with desktop | BRAND + `version:sync` |
| Device matrix (M1–M3) | **Not gated** for v1.5.4 desktop ship |
| Native components (push, signing, 16 KB) | **v1.5.5+** production mobile lane |

v1.5.4 **desktop** release does not require mobile to be marketed or signed for end users.

---

## Exit criteria

1. Automated gates in [v1.5.4-gate.md](../releases/v1.5.4-gate.md) green (desktop-focused).  
2. Desktop manual matrix: `docs/assets/demo/v1.5.4/manual-verification.md`.  
3. Visual check: installer icon ≠ Tauri default on Windows (minimum).  
4. `pnpm version:check` at `1.5.4` at tag time.  
5. Mobile device matrix **not** required for closeout.

---

## Investigation order

1. BRAND-1 → BRAND-4 (icons block user trust on install).  
2. WS-C relay dedupe.  
3. WS-D one P1 fix.  
4. Optional Android version alignment (no device test).

---

## Closeout

`CHANGELOG.md` `## [v1.5.4]`, GitHub Release, update [docs/README.md](../README.md) current release pointer, archive per [release-closeout-guide.md](../releases/release-closeout-guide.md).
