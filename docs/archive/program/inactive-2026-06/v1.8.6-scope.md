# v1.8.6 — Lane T: relay-join completion + membership read-model completion

**Status:** **Active** — development started (2026-05-27)  
**Predecessor:** [v1.8.5-release.md](../releases/v1.8.5-release.md)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md) · Lane **T**

---

## Goal

Close the remaining post-REL-004 gap in the invite-to-join flow and finish the read-model owner boundary:

- relay join after DM accept completes reliably (or fails with explicit evidence-backed status);
- membership/discovery views consume one canonical `CommunityMembershipReadModel` path;
- no resurrection/ghost outcomes from fallback read paths.

---

## Done when (v1.8.6 exit)

| ID | Task | Evidence |
|----|------|----------|
| T6-1 | **Relay join after accept** exits deterministic states (`joined`, `retry_scheduled`, `terminal_failed`) | Implemented in `community-invite-relay-join.ts` + component wiring/tests; local manual soak blocked by environment |
| T6-2 | Join retry loop emits honest user copy and no infinite spinner states | Implemented (`handleRelayJoinRetry`, terminal failure copy, retry owner state) |
| T6-3 | `CommunityMembershipReadModel` is owner for participants + network membership summary | Implemented (`use-community-membership-read-model-index` + consumer wiring/tests) |
| T6-4 | No parallel fallback owner reads remain in modal/network invite summary paths | Network/invite/header consumers ported; targeted grep + tests |
| T6-5 | Manual matrix pass for invite→accept→relay-join (Tester1/Tester2) | **Pending staging evidence** — local loopback environment blocked (`docs/assets/demo/v1.8.6/README.md`) |

---

## Out of scope (v1.8.7+)

- Lane X experience polish bundles.
- Lane K backend authority migration.
- Full public-relay parity guarantees beyond documented test environment.

---

## Guardrails

- Local state is not network truth.
- One owner per lifecycle/state/transport path.
- Do not claim relay parity without runtime + test agreement.

