# v1.8.7 — Lane T: transport-hard evidence + membership consistency hardening

**Status:** Active — T7 implementation in progress (2026-05-28)  
**Predecessor:** [v1.8.6-gate.md](../releases/v1.8.6-gate.md)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md) · Lane **T**

---

## Goal

Reduce reliance on environment-sensitive manual soak by expanding deterministic automated evidence:

- relay-join outcome contracts are validated in CI with transport-hard tests;
- membership surface consistency is validated from one read-model owner path;
- release confidence remains explicit about what is proven vs environment-blocked.

---

## Done when (v1.8.7 exit)

| ID | Task | Evidence |
|----|------|----------|
| T7-1 | Relay-join deterministic outcomes validated in a transport-hard automated slice | **Implemented** — `community-invite-relay-join.test.ts` expanded (transient retry success, manual retry terminal transitions, retry visibility guards) |
| T7-2 | Membership surface consistency assertions cover header/network/participants/invite modal flows | **Implemented** — tests added/expanded across `invite-to-group-dialog`, `network-dashboard`, and `chat-view` forwarding to `chat-header` |
| T7-3 | v1.8.7 scope/gate/demo/handoff docs aligned with real release confidence boundaries | **Implemented** — `v1.8.7` docs packet + handoff/roadmap sync complete |

---

## Out of scope (v1.8.8+)

- Full staging/public-relay manual soak completion.
- Lane X experience bundles.
- Lane K coordination-backend authority migration.

---

## Guardrails

- Local state is not network truth.
- One owner per lifecycle/state/transport path.
- No production parity claims without runtime + test agreement.

