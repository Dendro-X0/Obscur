# v1.8.12 — Lane P/R: release artifact parity + Android P1 wrap-up

**Status:** Superseded — **do not tag or publish v1.8.12** (use [v1.8.13-scope.md](./v1.8.13-scope.md))  
**Predecessor:** [v1.8.11-gate.md](../releases/v1.8.11-gate.md) (**shipped** — imperfect desktop installers)  
**Program:** [v1.8.x-release-train.md](./v1.8.x-release-train.md) · Lanes **P** + **R**  
**Release notes:** [v1.8.12-release.md](../releases/v1.8.12-release.md)  
**Gate:** [v1.8.12-gate.md](../releases/v1.8.12-gate.md)

---

## Goal

1. **R12** — **Release artifact parity:** desktop installers must match tag semver on Obscur Full Release (fix stale Cargo bundle cache; verify in CI).
2. **P12** — **Android P1 wrap-up:** emulator/device smoke, mobile shell UX pass, documented signing path — per [android-p1-signing-runbook.md](./android-p1-signing-runbook.md) and [stability-first-delivery.md](./stability-first-delivery.md).

No B2 inbound bot triggers in this patch unless explicitly pulled in later.

One patch tag when gates green — CI-only fixes on `main` without version bump per [version-line-policy.md](./version-line-policy.md).

---

## Context (v1.8.11 carry-forward)

| Issue | v1.8.11 state | v1.8.12 target |
|-------|----------------|----------------|
| Desktop installers labeled `1.8.10` on `v1.8.11` release | Known — stale bundle cache | Re-release with parity checks green |
| Android APK/AAB | Correct `1.8.11` filenames | Keep alignment via `version:sync` |
| B1 live Test B1 | Operator-deferred | Still deferred — not blocking P12 |

Workflow fixes landed on `main` after `v1.8.11` tag: `pnpm install` in reliability-gates, outbound bot ESM fix, release.yml cache key + bundle clear + **fail-fast** parity (no sed rename of stale installers).

---

## In scope

| ID | Task | Evidence | Status |
|----|------|----------|--------|
| R12-1 | Desktop installer **filename + build** parity with tag | Post-build `verify-desktop-bundle-filenames`; blocking full `release:artifact-version-parity` | In progress |
| R12-2 | Document v1.8.11 desktop caveat on release page | [v1.8.11-release.md](../releases/v1.8.11-release.md) note | Done |
| P12-1 | Android debug APK install + unlock smoke | [v1.8.12 demo](../assets/demo/v1.8.12/README.md) P12-1; `pnpm p12:android-smoke` | In progress |
| P12-2 | Mobile shell UX pass (safe area, nav, one community + DM path) | [mobile-ux-audit.md](../assets/demo/v1.8.12/mobile-ux-audit.md) + demo matrix checklist | Pending (manual; touch scroll + overlap backlog) |
| P12-3 | `pnpm verify:android-prerequisites` + emulator build in CI path | Prerequisites green locally; `build-android` CI job | Done (CI) / local build in flight |
| R12-3 | Tag + Obscur Full Release | `v1.8.12` artifacts | Pending |

---

## Out of scope (v1.8.13+ / v1.9.0)

- **B2/B3** inbound bot triggers and webhooks.
- Lane **K** coordination authority — **v1.9.0**.
- Public promotion — post **v2.0.0**.
- Play Console / commercial signing.

---

## Done when (v1.8.12 exit)

| Check | Requirement |
|-------|-------------|
| Release parity | All desktop + Android release filenames match `package.json` / tag |
| Android P12 | Install + cold start + one chat path on emulator (or device) |
| Automation | `docs:check`, typecheck, `release:test-pack`, branch CI green |
| Publish | Obscur Full Release artifacts for `v1.8.12` |
