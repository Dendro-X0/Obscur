# v1.5.0 — Architecture refactor checkpoint log

**Purpose:** Append-only progress index for the refactor track. See `docs/program/v1.5.0-refactor-verification-and-docs-policy.md` for verification rules and checkpoint duties.

---

## Log (newest first)

- **2026-05-15** — **REL-001 partial — leave intent resurrection guard:** `community-membership-leave-intent.ts`; recovery + coordinator backfill gated; `leaveGroup` enqueues outbox. Vitest REL-001 (3).
- **2026-05-15** — **REL-004 — leave outbox background retry:** `community-leave-outbox-retry.ts`, `use-community-leave-outbox-retry.ts` in `RuntimeActivationManager`; `publishViaRelayCore` scoped NIP-29 leave replay. Vitest retry (3) + outbox (25).
- **2026-05-15** — **REL-004 partial — leave outbox wired:** ledger-first (`leaveGroup` before relay) in management dialog + leave page; `enqueueCommunityLeaveOutboxItem` + `recordCommunityLeaveRelayPublishOutcome` in sealed leave + settings bulk-leave. Vitest leave-outbox (+2). `pnpm exec tsc --noEmit` (apps/pwa).
- **2026-05-15** — **Phase 3 M2 — relay bridge projection-only receive:** inbound gossip/anti-entropy no longer `setMembership`; authority via `community-membership-ingress`. **`persistExplicitCommunityMembershipLeave`** for settings bulk-leave. Vitest relay-bridge + coordinator; `pnpm exec tsc --noEmit` (apps/pwa).
- **2026-05-15** — **Phase 3 M3 — gate `runtime_invite_accepted`:** coordinator + `GroupProvider` — DM invite-accept provisional only; authoritative join via `relay_gossip_ingress`. Vitest: coordinator, M4 replay, group-provider (+1). `pnpm exec tsc --noEmit` (apps/pwa).
- **2026-05-15** — **Phase 3 ingress→coordinator:** `apply-community-membership-ingress.ts`, `subscribe-community-membership-ingress.ts`, `GroupProvider` subscriber; `relay_gossip_ingress`; invite `provisionalJoin`. Vitest ingress (2) + M4 (8) + group-provider (28); docs: implementation-plan Phase 3, phase3-scope, architecture queue, handoff.
- **2026-05-13** — **R2 — leave page sealed-community seeds:** **`groups/leave/page.tsx`** — **`leaveSealedCommunityInitialMembers`** + **`rosterLookupConversationId`** (**`group.id`** or **`toGroupConversationId`**); **`pnpm exec tsc --noEmit`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-13** — **R2 — `main-shell` sealed-community seeds:** **`main-shell.tsx`** **`sealedCommunityInitialMembers`** + **`resolveCommunitySeedMemberPubkeysFromDirectory`** for **`useSealedCommunity`** **`initialMembers`**; **`main-shell.test.tsx`** mock **`communityKnownParticipantDirectoryByConversationId`**. **`pnpm exec tsc --noEmit`** + **`pnpm exec vitest run app/features/main-shell/main-shell.test.tsx`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `resolveActiveCommunityMemberPubkeysFromConversation`:** **`community-visible-members.ts`** + test; **`group-home-page-client`** + **`group-management-dialog`** single **`useMemo`**; **`12-core-architecture-truth-map.md`**, **`14-module-owner-index.md`**, handoff. Vitest **`community-visible-members`** (12) + **`group-provider`**; **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**.
- **2026-05-14** — **R2 — author dedupe in `group-provider` + `collectGroupMessageAuthorPubkeys`:** **`resolveAuthorEvidencePubkeysFromCommunityMessages`**; **`community-message-author-evidence.ts`** delegates; **`group-provider`** hydrate paths. Vitest **`community-message-author-evidence.test.ts`** + **`group-provider.test.tsx`**; **`pnpm exec tsc --noEmit`** (apps/pwa); **`12-core-architecture-truth-map.md`** (R2 paragraph), **`14-module-owner-index.md`**, **`handoffs/current-session.md`**; **`pnpm docs:check`**.
- **2026-05-14** — **R2 — `resolveAuthorEvidencePubkeysFromCommunityMessages`:** **`community-visible-members.ts`** + **`community-visible-members.test.ts`**; **`group-home-page-client`** + **`group-management-dialog`**; truth map R2 interim + **`14-module-owner-index`** row; queue last updated. **`pnpm exec vitest run`** **`community-visible-members.test.ts`** (11); **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**; **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `use-stable-community-participant-pubkeys.ts`:** shared ref-sync + **`stabilizeCommunityMemberPubkeys`** for **`group-home-page-client`** + **`group-management-dialog`**; **`community-visible-members`** stabilize JSDoc points to hook. **`pnpm exec vitest run`** use-sealed-community integration + **`group-provider.test.tsx`** + **`community-visible-members.test.ts`**; **`pnpm exec tsc --noEmit`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — sealed-community single hook:** **`group-management-dialog.tsx`** now imports **`useSealedCommunity`** from **`use-sealed-community.ts`** (matches **`group-home-page-client`**); deleted **`use-sealed-community-fixed.ts`**. Removed redundant **`knownParticipantRegistry`** memo. **`pnpm exec vitest run`** `use-sealed-community.integration.test.ts` + **`group-provider.test.tsx`**; **`pnpm exec tsc --noEmit`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — snapshot policy diagnostics:** **`resolveEnhancedSnapshotPolicyReasonCode`** in **`community-member-snapshot-policy.ts`** — `policyReasonCode` no longer labels **`relay_evidence_seed_only_allowing_thinner`** when the guard did not relax (adds **`relay_evidence_relax_blocked_protected_member`**, **`relay_evidence_warming_up_strict`**). **`community-member-snapshot-policy.test.ts`** (+2 cases). **`pnpm exec vitest run`** snapshot-policy + relay-evidence-policy; **`pnpm exec tsc --noEmit`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — hygiene:** removed **`[MemberStabilization]`** / **`[MemberFix]`** `console.log` noise from **`group-home-page-client.tsx`** and **`group-provider.tsx`** (roster merge still unchanged). **`pnpm exec tsc --noEmit`** (apps/pwa); **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `community-visible-members.ts`:** **`resolveCommunitySeedMemberPubkeysFromDirectory`** (`mergeKnownParticipantSeedPubkeys` + **`resolveCommunitySeedMemberPubkeys`**); **`group-home-page-client`** + **`group-management-dialog`** use it for sealed-community member seeds. **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**; **`14-module-owner-index.md`**, **`12-core-architecture-truth-map.md`**, **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `group-provider.tsx` + `community-member-snapshot-policy.ts`:** relay snapshot **`protectRemovalPubkeys`** now uses **`mergeKnownParticipantSeedPubkeys`** (directory ∪ persisted **`memberPubkeys`**) instead of ad-hoc **`dedupePubkeys`** concat; policy JSDoc notes contract alignment with UI seeds. **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**; **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `group-home-page-client.tsx`:** single **`seededMemberEvidence`** **`useMemo`** feeds **`useSealedCommunity({ initialMembers })`** and downstream visible-member inputs (removed duplicate inline **`resolveCommunitySeedMemberPubkeys`** / **`mergeKnownParticipantSeedPubkeys`** at sealed-community boundary). **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**; **`12-core-architecture-truth-map.md`**, **`handoffs/current-session.md`**.
- **2026-05-14** — **R2 — `group-provider.tsx`:** removed **`createdGroups`** multi-member **`useEffect`** that **called** **`upsertCommunityKnownParticipantsEntry`** from **`group.memberPubkeys`** when `memberCount > storedCount` (bypassed directory minimal-baseline gate). Durable writes remain: directory build + persist only when sorted directory ≠ stored ∪ `memberPubkeys` ∪ self. **`pnpm exec tsc --noEmit`** (apps/pwa); **`pnpm docs:check`**; **`12-core-architecture-truth-map.md`**, **`v1.5.0-architecture-refactor-queue.md`**, **`handoffs/current-session.md`**.
- **2026-05-13** — **R1 — `dm-conversation-hydrate-pipeline.ts`:** **`runDmConversationHydrateReadModelPipeline`** (tombstone prep → Indexed initial window → persisted **`chat-state-store`** fallback → **`assembleDmHydrateThreadReadModel`** → **`runDmHydrateSiblingIdSplitDiagnosticsIfNeeded`**) + **`logDmHydrateReadModelTelemetry`**; **`use-conversation-messages`** **`hydrateHistory`** delegates (persisted fallback helper moved out of hook). **No new Vitest** this slice per active policy — **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`v1.5.0-architecture-refactor-queue.md`**, **`12-core-architecture-truth-map.md`**, **`14-module-owner-index.md`**, **`handoffs/current-session.md`** updated.
- **2026-05-13** — **R1 — `dm-conversation-displayable-message.ts`:** **`isVoiceCallSignalPayload`**, **`isDisplayableDmConversationMessage`** — deduped identical copies from **`use-conversation-messages`** + **`dm-conversation-hydrate-read-model`**. **`dm-conversation-displayable-message.test.ts`** (6). **`pnpm exec vitest run`** displayable + hydrate read-model + **`use-conversation-messages`**.test + integration + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`14-module-owner-index.md`**, **`dm-conversation-hydrate-indexed-map-rows.ts`** comment, **`v1.5.0-architecture-refactor-queue.md`**, handoff.
- **2026-05-13** — **R1 — `dm-conversation-normalize-message.ts`:** **`normalizeDmConversationMessageRow`** extracted from **`use-conversation-messages`** (hydrate **`mapRows`**, projection evidence, sibling diagnostics, **`loadEarlier`**, **`applyBufferedEvents`**). **`dm-conversation-normalize-message.test.ts`** (5). **`pnpm exec vitest run`** normalize + **`use-conversation-messages`**.test + integration + projection-evidence + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`dm-conversation-hydrate-indexed-scan.ts`** / **`dm-conversation-hydrate-indexed-map-rows.ts`** header comments; **`14-module-owner-index.md`**, **`v1.5.0-architecture-refactor-queue.md`**, **`handoffs/current-session.md`** updated.
- **2026-05-13** — **R1 — `dm-conversation-projection-live-merge.ts`:** **`mergeProjectionFirstWithLiveOverlayForDisplay`** + **`areMessageListsEquivalentById`** (extracted from **`use-conversation-messages`** projection merge **`useEffect`**; adds **`conversationAliasIdSet`** to effect deps). **`dm-conversation-projection-live-merge.test.ts`** (6). **`pnpm exec vitest run`** this file + **`use-conversation-messages.integration.test.ts`** + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`14-module-owner-index.md`**, **`v1.5.0-architecture-refactor-queue.md`**, **`handoffs/current-session.md`** updated.
- **2026-05-13** — **R1 — `dm-conversation-projection-evidence-messages.ts`:** **`buildProjectionEvidenceMessagesForConversation`** (`selectProjectionConversationMessages` + **`filterMessagesBySuppressedIds`** + **`filterMessagesByLocalRetention`**); **`use-conversation-messages`** `projectionEvidenceMessages` useMemo delegates; hook supplies **`normalizeMessage`**. **`dm-conversation-projection-evidence-messages.test.ts`** (3, mocked selector). **`pnpm exec vitest run`** this file + **`use-conversation-messages.integration.test.ts`** + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`12-core-architecture-truth-map.md`**, **`14-module-owner-index.md`**, **`v1.5.0-architecture-refactor-queue.md`** updated.
- **2026-05-13** — **R1 — `dm-conversation-message-retention-dedupe.ts`:** **`filterMessagesByLocalRetention`**, **`dedupeMessagesByIdentity`**, **`normalizeLocalRetentionDays`** moved from hook; **`dm-conversation-hydrate-indexed-map-rows`** imports them (no injectable dedupe/retention). **`dm-conversation-message-retention-dedupe.test.ts`** (3). Hook tests drop duplicate retention describe. **`pnpm exec vitest run`** retention + map-rows + hook unit/integration + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`12-core-architecture-truth-map.md`**, **`14-module-owner-index.md`**, **`v1.5.0-architecture-refactor-queue.md`** updated.
- **2026-05-13** — **R1 — sibling id-split diagnostics:** **`utils/dm-conversation-sibling-ids.ts`** (`inferPeerFromConversationId`, `buildDmSiblingConversationIds` moved from hook); **`dm-conversation-hydrate-sibling-diagnostics.ts`** (`runDmHydrateSiblingIdSplitDiagnosticsIfNeeded`); **`dm-conversation-sibling-ids.test.ts`** (3). Hook calls service after **`assembleDmHydrateThreadReadModel`**. **`pnpm exec vitest run`** sibling ids + **`use-conversation-messages.integration.test.ts`** + **`pnpm exec tsc --noEmit`** (apps/pwa) green; **`12-core-architecture-truth-map.md`**, **`14-module-owner-index.md`**, **`v1.5.0-architecture-refactor-queue.md`** updated.
- **2026-05-13** — **R1 — `dm-conversation-hydrate-indexed-map-rows.ts` (superseded by retention module):** **`mapIndexedConversationRowsForDisplayableScan`** centralizes **`initial_hydrate`** vs **`load_earlier`** filter order; hook originally injected dedupe/retention — see newer checkpoint **`dm-conversation-message-retention-dedupe.ts`**. **`dm-conversation-hydrate-indexed-map-rows.test.ts`** (3).
- **2026-05-13** — **R1 — `dm-conversation-hydrate-indexed-scan.test.ts`:** empty-alias idempotence, **`eventId`** dedupe keeps newer timestamp, **`loadInitialDmHydrationIndexedWindow`** second IDB window when **`targetVisibleCount`** exceeds first batch (mocked **`getAllByIndex`**). **`pnpm exec vitest run`** this file + **`pnpm exec tsc --noEmit`** (apps/pwa) green.
- **2026-05-13** — **R1 — `dm-conversation-hydrate-indexed-scan.ts`:** SQLite/IndexedDB **`loadConversationWindow`**, alias merge + **`scanDisplayableHistoryWindow`**, **`loadInitialDmHydrationIndexedWindow`** (initial window + scan + soft cap). **`use-conversation-messages`** imports these; duplicate inline window helpers removed. **`dm-conversation-hydrate-read-model.test.ts`** `mkMsg` duplicate-key TS2783 fix. **`pnpm exec tsc --noEmit`** (apps/pwa) + **`vitest run`** `use-conversation-messages*.test.ts` + `dm-conversation-hydrate-read-model.test.ts` green.
- **2026-05-14** — **R1 — `dm-conversation-hydrate-read-model.ts`:** pure **`assembleDmHydrateThreadReadModel`** (authority + cap + group scope + live overlay + log contexts); **`getMessageDirectionCounts`** / **`toConversationIdDiagnosticLabel`** moved from hook; **`use-conversation-messages`** `hydrateHistory` calls assembler after IDB scan. **`dm-conversation-hydrate-read-model.test.ts`** (2). **`pnpm exec tsc --noEmit`**, integration + contract Vitest + **`pnpm docs:check`** green.
- **2026-05-14** — **R1 — truth-map / queue clarity (no code):** **`docs/program/v1.5.0-architecture-refactor-queue.md`** — interim milestones vs exit; **`docs/encyclopedia/14-module-owner-index.md`** — split DM hydrate choke vs hook assembler; **`docs/encyclopedia/12-core-architecture-truth-map.md`** — materialization module named in R1 bullet; **`docs/handoffs/current-session.md`** Next Atomic Step numbering restored. **`pnpm docs:check`** green.
- **2026-05-14** — **R1 — removed `conversation-history-authority.ts`:** production + tests use **`resolveLegacyHydrationAuthority`** / **`resolveHydrationDmReadMessages`** in **`dm-read-authority-contract.ts`** only; **`conversation-history-authority.test.ts`** unchanged path (**15** tests). **`HEURISTIC_PATH_QUARANTINE.md`** Entry 1 marked removed. **`docs/rewrite/**`** owner-set backtick refs updated (**`dm-read-authority-contract.ts`**). **`pnpm exec tsc --noEmit`** + authority + contract Vitest + **`pnpm docs:check`** green.
- **2026-05-14** — **R1 — hook on contract hydrate:** `use-conversation-messages` uses **`resolveHydrationDmReadMessages`** + **`logDmReadHydrationDiagnostics`**; persisted short-circuit + always **`legacyAuthorityDecision`** (see prior checkpoint detail in git history if needed).
- **2026-05-14** — **R1 — quarantine import surface narrowed (superseded by file removal):** **`conversation-history-authority-shared.ts`** holds types + **`PERSISTED_INCOMING_REPAIR_INDEXED_MESSAGE_MAX`**, thinness + restore-phase repair gate helpers. **`conversation-message-materialization`** and **`use-conversation-messages`** import shared for those symbols; **`conversation-history-authority.ts`** was later deleted (see newest log entry). **`pnpm exec tsc --noEmit`** + **`conversation-history-authority.test.ts`** green at time of entry.
- **2026-05-14** — **R2 — defer synthetic known-participants localStorage sync:** `group-provider` bulk effect skips **`upsertCommunityKnownParticipantsEntry`** when in-memory directory equals **stored ∪ `group.memberPubkeys` ∪ local** only (sorted-set compare); roster / relay-widened unions still persist. **`community-known-participants-store`** module note lists writers. **`pnpm exec tsc --noEmit`** + **`group-provider.test.tsx`** (28) green.
- **2026-05-14** — **R2 — snapshot test + provider comment:** `group-provider` membership snapshot `protectRemovalPubkeys` comment ties to **`mergeKnownParticipantSeedPubkeys`** (UI seed OR-set); snapshot path unchanged (group + directory concat). **`group-provider.test.tsx`** “mu” case renamed and expectations aligned with **rejected** thin snapshots without leave/expel (`missing_removal_evidence`, roster stays `[A,B]`). `pnpm exec vitest run` group-provider + known-participant-directory tests green.
- **2026-05-14** — **R2 — known-participant seed read path:** **`mergeKnownParticipantSeedPubkeys`** in **`community-known-participant-directory.ts`** (deduped OR-set union: directory + persisted **`group.memberPubkeys`** for bootstrap when directory row missing). **`group-home-page-client.tsx`** + **`group-management-dialog.tsx`** use it for **`useSealedCommunity`** initial seed and **`seededMemberEvidence`** (removed duplicate manual spreads). **`community-known-participant-directory.test.ts`** (3 tests). **`pnpm exec tsc --noEmit`** + vitest green.
- **2026-05-14** — **R2 — single `stabilizeCommunityMemberPubkeys` implementation:** canonical logic only in **`community-member-roster-projection.ts`** (docstring). **`community-visible-members.ts`** delegates (`previous`/`next` → `current`/`incoming`); removed ~50 lines of duplicate guard logic. **`community-visible-members.test.ts`** + **`community-member-roster-projection.test.ts`** green; **`pnpm exec tsc --noEmit`** (apps/pwa) green.
- **2026-05-13** — **R1 — DM hydrate bridge unified:** `resolveHydrationMessagesViaDmReadBridge` handles **null and non-null** `identityPubkey` (legacy selection inside bridge); hook **`use-conversation-messages`** no longer imports **`resolveConversationHistoryAuthority`** / **`selectMessagesForConversationHistoryAuthority`** for hydrate. **`persistedOutgoingCount`** passed through legacy parity with former hook else-branch. **`legacyAuthorityDecision`** on bridge result preserves granular **`conversation_history_authority_selected`** logs when identity is absent. **`resolveDmReadAuthorityViaBridge`** / **`selectMessagesViaBridge`** delegate to **`resolveHydrationMessagesViaDmReadBridge`**. Bridge Vitest +3 (null-identity case). `pnpm exec tsc --noEmit` (apps/pwa) green.
- **2026-05-14** — **R1 — DM read bridge in hydrate:** `resolveHydrationMessagesViaDmReadBridge` in `dm-read-authority-migration-bridge.ts` (single legacy→contract→select path); **`legacyProjectionEvidenceMessageCount`** fixes legacy parity vs projection row count; **`selectMessagesViaBridge`** / **`resolveDmReadAuthorityViaBridge`** share **`buildMigrationDmReadAuthorityParams`**. **`use-conversation-messages`** `hydrateHistory` uses bridge when **`normalizedPublicKeyHex`** is set, else legacy authority + **`selectMessagesForConversationHistoryAuthority`**. New **`dm-read-authority-migration-bridge.test.ts`**. `pnpm exec tsc --noEmit` + bridge vitest green.
- **2026-05-14** — **R1:** `conversation-message-materialization.ts` — `selectMessagesForConversationHistoryAuthority`, `capMessageListToSoftLiveWindow`; `use-conversation-messages` hydrate path uses them after `resolveConversationHistoryAuthority`. Materialization tests at 8.
- **2026-05-14** — **R1:** `mergeProjectionFirstWithOverlayMessages`, `mergeHydratedBaseWithLiveOverlayMessages`, `filterMessagesBySuppressedIds`; hook + `applyBufferedEvents` / persisted fallback aligned on suppression identity.
- **2026-05-14** — **R1:** `conversation-message-visibility.ts` + `account-projection-selectors` / `use-conversation-messages` suppression contract; **R2-adjacent:** `community-member-snapshot-policy` iterable fix for `protectRemovalPubkeys`.
