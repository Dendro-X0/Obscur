# v1.3.8 Raw Evidence

Place raw replay artifacts here, for example:

1. offline replay logs (`*.log`, `*.json`),
2. updater replay command output and diagnostics dumps,
3. screenshot references used by the manual checklist.
