# v1.8.14 Release Notes

**Codename:** Lane T/P/R — v1.8.x batch exit (P13 + B2 + Wave 3 trust)  
**Status:** **Build-only on GitHub** — tag `v1.8.14` may exist; **Latest** release remains **v1.8.11** (R13: publish requires manual `publish_release`). Maintainer **deferred** GitHub publish; proceeding **v1.9.0** Lane K.  
**Builds on:** [v1.8.11](./v1.8.11-release.md) (**shipped** — superseded by this release)

---

## Summary

**v1.8.14** is the **v1.8.x batch exit** patch: mobile shell polish (P13), **inbound community bot triggers** (B2), trust slices (REL/MEM), release CI parity (R12/R13 carried), and light-theme / layout polish carried from Wave 4 (P14 partial).

This release supersedes the void **`v1.8.12`** tag and corrects the **v1.8.11 desktop installer version mismatch** via R12 artifact parity on `main`.

**Production posture:** GitHub **Latest** remains **v1.8.11** until maintainer runs **Obscur Full Release** with `publish_release=true` and `release_tag=v1.8.14` after batch commits are on the tag.

---

## Highlights

### B2 — Inbound community bots

- **`botTriggers`** descriptor contract — keyword, mention, and schedule triggers on managed workspaces.
- **`scripts/community-inbound-bot.mjs`** — subscribe, match, reply via sealed publish; crypto decrypt tests.
- **Steward UX** — enable/disable triggers per bot in Manage hub.
- **Runbook** — [community-inbound-bot.md](../messaging/community-inbound-bot.md).

### P13 — Mobile shell + chat-thread polish

- **Status strip** — suppress restore/sync/relay notices during account load; scope mismatch still shown.
- **Conversation row** — timestamp beside preview; direction-aware invitation previews; sidebar formats structured invite/response JSON at render time with peer name and correct recency.
- **Composer** — removed amber relay queue / cooling-down footer banners.
- **Message list** — mobile action dock below bubble; sustained touch (~420ms) shows dock like hover.

### P14 — Light theme + layout polish (carried)

- **Dialog / ui-kit** — Tailwind v4 scan fix so modal content stays in viewport; overlay z-index aligned.
- **Group management + community home** — light/dark contrast pairs on panels and bento cards.
- **Network profile** — full-width connection status stack; hero online badge; NIP-05 inline in public identity.
- **Community invite cards** — light-theme historical cards; community avatar in card + details dialog.
- **Auth screen** — primary actions use purple scheme (login / continue / welcome).
- **iPad / tablet** — secondary-page layout tier; settings split nav; capped invite card width on tablet.

### Wave 3 — Trust slices

- **REL-003** — Named profiles no longer seed shared legacy membership ledger key; bootstrap restore preserves fresh-device rebind.
- **REL-004** — Leave outbox durability verified; settings bulk-leave audit path corrected.
- **MEM-002** — Membership read-model index loads terminal left/expelled cache; window listeners filter by `profileId`.
- **MEM-005** — Inviter terminal invite (declined/canceled) clears relay-joined roster via `group-invite-terminal` bus.

### R12/R13 — Release CI (carried)

- **Obscur Full Release** — `workflow_dispatch` rebuild without new tag; optional `release_tag` for publish from `main`.
- **Artifact parity** — desktop + Android version checks; AAB binary fallback when bundle metadata rows missing.

---

## Sidebar preview authority (P13 fix)

- **Projection merge** — `buildLatestConversationByPeer` scans full message timeline; reducer only updates preview when message is latest; stale connection overrides ignored.
- **Persisted history** — SQLite + chat-state merge refreshes sidebar when message history changes.
- **MessagingProvider** — revision memo tied to projection sequence + chat-state replace events.

---

## Accepted limitations (unchanged)

| ID | Theme |
|----|--------|
| DM-001 | One-sided DM outgoing history on restore |
| MEM-001 | (parked with DM-001) |

Manual demo matrices (P13 soak, B-02 inbound keyword, cross-surface MEM-002) remain **deferred** — see [deferred-manual-verification-checklist.md](../program/deferred-manual-verification-checklist.md).

---

## Upgrade from v1.8.11

1. Install desktop/Android artifacts from the **v1.8.14** GitHub Release (once published).
2. Desktop installers will show **1.8.14** semver (fixes v1.8.11 stale **1.8.10** filename issue).
3. No migration steps required for local data.

---

## Publish checklist (maintainer)

1. Commit batch exit payload on `main` (P13 + B2 + Wave 3 + UI polish).
2. `pnpm version:check && pnpm docs:check && pnpm release:test-pack -- --skip-preflight`
3. Move **`v1.8.14`** tag to the commit that includes the full payload (delete remote tag if it only carries CI fixes).
4. **Actions → Obscur Full Release** → verify green.
5. Re-run with `publish_release=true`, `release_tag=v1.8.14`.

---

## Links

- [v1.8.14 gate](./v1.8.14-gate.md)
- [v1.8.14 scope](../program/v1.8.14-scope.md)
- [v1.8.14 demo matrix](../assets/demo/v1.8.14/README.md)
- [v1.8.x release train](../program/v1.8.x-release-train.md)
- [CHANGELOG v1.8.14](../../CHANGELOG.md)
