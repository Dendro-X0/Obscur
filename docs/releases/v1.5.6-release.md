# v1.5.6 Release Notes

**Codename:** Mobile Preview (planned) + desktop shared kernel  
**Status:** Shipped (documentation + desktop kernel on `main`) — **Lane M paused**; optional **Git tag** when maintainer runs [v1.5.6-gate.md](./v1.5.6-gate.md) A1–A4 + D1  
**Builds on:** [v1.5.5](./v1.5.5-release.md) (Client Depth)  
**Scope:** [v1.5.6-scope.md](../program/v1.5.6-scope.md) · **Next:** [v1.5.7-scope.md](../program/v1.5.7-scope.md)

---

## Summary (planned)

First **mobile UI** milestone: DM-first navigation in the WebView shell (auth → list → thread) reusing the shared messaging kernel. **Desktop/web** ship **Phase 1 community** and R/U refinements on the same `1.5.6` version line while the mobile product matrix is paused.

---

## Desktop / shared kernel (same tag)

- **Reliability:** R4 idle deferral (settings/search); R5 invite-decline roster edge case.
- **UX:** U1 invite banners; themed `SelectField` (including modal-safe portal menu).
- **Community Phase 1:** sealed descriptor updates + `descriptor_updated` ledger reason; sealed governance (propose / vote / resolve / TTL `expired`); Governance tab + home banner; session governance cache; Network display names via `resolveCommunityDisplayName`; active roster for home/management/invite.

---

## Planned highlights

### M1 — DM-first layout

- Mobile shell routes and layout for conversation list + thread.
- Shared hooks/controllers only — no second send path.

### M2 — Shell polish

- Safe areas and tab bar spacing on Android emulator.

### Distribution

- Optional unsigned APK on GitHub Release when tag ships.
- Signing / stores **not** required.

---

## Non-goals

- App Store / Play production gates.
- Full mobile feature parity with desktop.

---

## Verification

See [v1.5.6-gate.md](./v1.5.6-gate.md).
