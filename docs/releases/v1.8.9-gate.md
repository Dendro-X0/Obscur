# v1.8.9 — Release gate

**Status:** **Shipped** (2026-05-28)  
**Scope:** [v1.8.9-scope.md](../program/v1.8.9-scope.md)  
**Release notes:** [v1.8.9-release.md](./v1.8.9-release.md)  
**Roadmap:** [v1.8.9-plus-managed-workspace-roadmap.md](../program/v1.8.9-plus-managed-workspace-roadmap.md)

---

## Milestone (feature — not CI-only)

| Theme | Deliverable | Gate evidence |
|-------|-------------|---------------|
| **D1** | Operator relay suppresses community-scoped hide in live serve path | Automated tests + manual **Test D1** in demo matrix |
| **D3** | Strict workspace UX when relay is operator-trusted | Copy review + UI check in demo matrix |
| **Baseline** | Managed workspace Test 8 on current `main` | **Re-run** — [v1.8.9 demo matrix](../assets/demo/v1.8.9/README.md) |

---

## Automated gates (required before tag)

| # | Gate | Command |
|---|------|---------|
| G1 | Version sync | `pnpm version:check` → **1.8.9** |
| G2 | Docs integrity | `pnpm docs:check` |
| G3 | Typecheck | `pnpm -C apps/pwa typecheck` |
| G4 | Release test pack | `pnpm release:test-pack -- --skip-preflight` |
| G5 | D1 automated slice | `community-relay-hide-suppress.test.ts` + `pnpm ci:community-relay-hide-smoke` |
| G6 | Branch CI | `docs-check`, `reliability-gates` green on `main` |

**CI/workflow fixes** must be green on `main` during development; they are **not** sufficient reason to tag without T9-1/T9-2.

---

## Manual gates

| # | Gate | Evidence |
|---|------|----------|
| M9-1 | Test 8 — managed workspace A/B | ☑ [demo matrix](../assets/demo/v1.8.9/README.md) |
| M9-2 | Test D1 — hide suppress | ☑ `suppress_ok` |
| M9-3 | Test D3 — strict workspace copy | ☑ |
| M9-4 | B-side hydration + leave count | ☑ (Test 8) |

---

## Tag and publish (only when milestone complete)

```bash
git checkout main && git pull
pnpm version:check && pnpm docs:check
pnpm release:test-pack -- --skip-preflight
pnpm -C apps/pwa typecheck

git tag -a v1.8.9 -m "v1.8.9 — D1 relay suppress + managed workspace"
git push origin v1.8.9
```

Wait for **Obscur Full Release** → `publish-release` with platform artifacts.

---

## CI vs full release

| Trigger | Proves |
|---------|--------|
| Push to `main` | Docs + reliability slices — **not** installers |
| Push tag `v1.8.9` | Full matrix + **publish-release** |

If preflight fails after tag: **fix on `main`**, then `git tag -f v1.8.9` — do **not** bump to `v1.8.10` for CI-only fixes.

---

## Sign-off

| Role | Date | Notes |
|------|------|-------|
| Maintainer | | Test 8 + Test D1 + D3 |
| Release | | Obscur Full Release artifacts |
