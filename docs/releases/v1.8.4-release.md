# v1.8.4 Release Notes

**Codename:** Lane T — REL-004 leave durability + community invite DM  
**Status:** Shipped — **v1.8.4** (2026-05-27)  
**Builds on:** [v1.8.2](./v1.8.2-release.md)

---

## Summary

Public release **skips v1.8.3** and ships the Lane T slice as **v1.8.4**. A user's **leave** is durable **locally** before any relay publish. Inviter DM threads show honest community invite evidence (outbound cards, supersede, acceptance recorded). Coordination-backed roster truth removes ghost leavers from participant surfaces.

---

## Highlights

- **REL-004** — Canonical leave paths enqueue outbox **before** relay attempt.
- **T4-2 / T4-2b** — Left state authoritative in UI; observer roster drops leavers.
- **T4-3 / T4-4** — Pending outbox UX + background retry.
- **Community invite DM** — `inviteId` on wire, pipeline/ledger, inviter thread UX (timestamps, avatar).
- **Membership truth** — Coordination directory display; no monotonic ghost roster when coordination is configured.
- **CI** — `release:test-pack` green on `main`.

---

## Verification

| Gate | Result |
|------|--------|
| Manual REL-004 + DM invite soak | Pass (2026-05-27, Tester1/Tester2 desktop) |
| Automated | `version:check`, typecheck, community invariants, `release:test-pack` — Pass |

---

## Out of scope (v1.8.5+)

- Relay-backed group join after DM accept (`Complete join on relay` retry loop).
- Guaranteed group chat on misconfigured community relay — environment + Lane K.
- Full `CommunityMembershipReadModel` port across all surfaces.

---

## Next

**v1.8.5** — Relay join after accept + membership read-model completion.
