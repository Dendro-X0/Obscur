# v1.5.6 — Mobile Preview Gate

**Status:** **Paused** — automated gates apply to desktop patches; **product mobile gates deferred** until signing/install  
**Scope:** [v1.5.6-scope.md](../program/v1.5.6-scope.md)  
**Release notes:** [v1.5.6-release.md](./v1.5.6-release.md)

---

## Scope table

| In scope | Out of scope |
|----------|----------------|
| M1 DM-first layout in mobile shell | Store signing purchase |
| M2 safe-area / tab bar polish | Native UI rewrite |
| Emulator smoke notes | Mandatory F-Droid |
| Same version as desktop at tag | Separate mobile semver |

---

## Automated gates (required)

| # | Gate | Command |
|---|------|---------|
| A1 | Typecheck | `pnpm -C apps/pwa exec tsc --noEmit --pretty false` |
| A2 | Docs | `pnpm docs:check` |
| A3 | Release test pack | `pnpm release:test-pack` |
| A4 | Version alignment | `pnpm version:check` at `1.5.6` |

---

## Product gates — mobile (**deferred** — not required for desktop-only patches)

| # | Gate | Verification |
|---|------|----------------|
| M1 | Unlock → list → open DM thread | **Deferred** — blocked on signing/install |
| M2 | Tab bar / safe areas | **Deferred** |
| M3 | Send/receive one message | **Deferred** |

Matrix: `docs/assets/demo/v1.5.6/README.md` — run when Lane M resumes.

**Desktop patches** on `main` may ship in `v1.5.5.x`-style cadence or accumulate for `v1.5.6` when mobile gates reopen.

---

## Product gates — desktop (regression)

| # | Gate | Verification |
|---|------|----------------|
| D1 | v1.5.5 relay + vault regressions | Spot-check or prior manual matrices |

---

## Tag criteria

- M1 usable in emulator with notes committed.
- `release:test-pack` green on clean `main`.
