# Hero showcase drop-in

Place a narrated product demo here when ready:

- `showcase.mp4` — preferred landing hero stage (audio OK; site uses controls, no autoplay sound)
- `showcase.poster.jpg` — optional poster frame

Until these files exist, the landing falls back to a muted guide-media ambient clip.
