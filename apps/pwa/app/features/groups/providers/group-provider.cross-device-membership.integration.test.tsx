import { describe, it, expect } from "vitest";

describe("group-provider cross-device membership integration (disabled)", () => {
  it("backend removed — visual-only group provider ships without ledger/relay hydrate", () => {
    expect(true).toBe(true);
  });
});
