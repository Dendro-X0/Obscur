"use client";

/** Workspace-kernel era group list provider — legacy impl; features import this port only. */
export {
  LegacyGroupProvider,
  useGroups,
  useGroupsSafe,
} from "./group-provider-legacy";

export type { GroupContextType } from "./group-provider-types";
