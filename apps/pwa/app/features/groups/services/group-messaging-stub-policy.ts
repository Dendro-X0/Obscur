/** Canonical copy when outbound group send is blocked (visual-only backend). */
export const GROUP_MESSAGING_STUB_MESSAGE =
  "Group messaging is unavailable while the community backend is being rebuilt.";
