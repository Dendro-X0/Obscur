"use client";

/** Stable facade — implementation quarantined in `-legacy` (W52). */
export {
  publishTransportKernelToRelay,
  publishTransportKernelToRelayUrls,
} from "./transport-kernel-standalone-publish-legacy";
