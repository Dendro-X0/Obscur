/**
 * DM delete-for-everyone is **not** release-ready on current architecture.
 * Cooperative redaction remains in code for experiments; UI stays off until Gateway R1 + id contract v2.
 */
export const DM_DELETE_FOR_EVERYONE_UI_ENABLED = false;
