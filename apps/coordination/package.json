{
  "name": "@obscur/coordination-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "wrangler d1 execute obscur --local --file=./schema.sql",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 execute obscur --local --file=./schema.sql",
    "db:purge-membership": "wrangler d1 execute obscur --local --file=./scripts/purge-membership-directory.sql"
  },
  "dependencies": {
    "@dweb/coordination-contracts": "workspace:*",
    "@dweb/crypto": "workspace:*",
    "@noble/curves": "^1.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.18",
    "wrangler": "^3.106.0"
  }
}